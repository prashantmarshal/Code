#include <bits/stdc++.h>
using namespace std;

int size = 10;

int main(int argc, char const *argv[]) {
    vector<int> arr;
    for (int i = 0; i < size; i++) {
        arr.push_back(rand() % 100)
    }
}